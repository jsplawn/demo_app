Publishing checklist

RAILS_ENV=production bundle exec rake assets:precompile

bundle install
bundle exec rake db:migrate

git add .
git commit -a -m "message"
git push

git push heroku master
heroku rake db:migrate
//Opt heroku rake db:push